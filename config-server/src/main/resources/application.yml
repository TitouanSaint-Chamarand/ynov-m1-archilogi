server:
  port: 8880

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TitouanSaint-Chamarand/config-server-ynov-m1.git
          default-label: main